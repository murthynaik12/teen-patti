import "./globals.css";

export const metadata = {
  title: "Teen Patti Settlement",
  description: "Track every game and settle the table with clarity.",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}